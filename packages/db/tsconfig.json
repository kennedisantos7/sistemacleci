{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "prisma/**/*.ts"],
  "comment": "seed-catalog.ts importa arquivos do app 'site' (JSX/React) só para migração one-off via tsx; fora do typecheck do pacote.",
  "exclude": ["prisma/seed-catalog.ts"]
}
